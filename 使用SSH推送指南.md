# 使用SSH推送指南（22端口）

## 📋 问题说明

- **443端口被阻止**：无法使用HTTPS推送
- **22端口可用**：SSH端口可以连接
- **需要配置**：SSH密钥认证

## 🔑 配置SSH密钥

### 步骤1：生成SSH密钥

在Git Bash或PowerShell中执行：

```bash
# 生成SSH密钥（使用你的GitHub邮箱）
ssh-keygen -t ed25519 -C "your_email@example.com"

# 或者使用RSA（如果ed25519不支持）
ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
```

**提示**：
- 按Enter使用默认路径：`C:\Users\Administrator\.ssh\id_ed25519`
- 可以设置密码，也可以直接按Enter跳过

### 步骤2：启动SSH代理

```bash
# 启动ssh-agent
eval "$(ssh-agent -s)"

# 添加SSH密钥
ssh-add ~/.ssh/id_ed25519
```

### 步骤3：复制公钥

```bash
# 显示公钥内容
cat ~/.ssh/id_ed25519.pub

# 或者使用PowerShell
Get-Content $env:USERPROFILE\.ssh\id_ed25519.pub
```

### 步骤4：添加到GitHub

1. 复制公钥内容（以 `ssh-ed25519` 或 `ssh-rsa` 开头）
2. 登录GitHub
3. 进入 Settings → SSH and GPG keys
4. 点击 "New SSH key"
5. 粘贴公钥内容
6. 点击 "Add SSH key"

### 步骤5：测试连接

```bash
ssh -T git@github.com
```

如果看到 "Hi MRYGP! You've successfully authenticated..." 说明配置成功。

### 步骤6：使用SSH推送

```bash
# 设置远程URL为SSH
git remote set-url origin git@github.com:MRYGP/sanxiangwendao.git

# 推送
git push origin master --force
```

## 🚀 快速配置脚本

我已经创建了自动化配置脚本，可以帮你快速配置：

```bash
# 运行配置脚本
python scripts/setup_ssh.py
```

## ⚠️ 注意事项

1. **SSH密钥是敏感信息**：不要分享私钥（`id_ed25519`）
2. **公钥可以分享**：公钥（`id_ed25519.pub`）可以添加到GitHub
3. **如果已有SSH密钥**：可以直接使用，无需重新生成

## 📝 当前状态

- ✅ 22端口可连接
- ❌ 未配置SSH密钥
- ✅ 443端口被阻止（无法使用HTTPS）

配置SSH密钥后，就可以使用22端口推送了！

# AI 协作治理协议

> 适用对象：松阳、Claude Desktop、Codex 及未来接入本仓库的 AI 工具。
> 版本：v1.0
> 日期：2026-07-13
> 原则：一套共同规则、两个产品入口、一个本地事实源、一个默认写入者。

## 一、共同事实源

`D:\sanxiangwendao` 当前本地工作区是项目内容与执行状态的默认事实源。仓库移动到其他位置时，以实际工作区根目录为准。

GitHub 是版本同步、备份和协作渠道，不自动代表本地最新状态。Claude Projects 过去上传的仓库文件是历史快照，不再作为项目事实源。

涉及项目观点、文件状态、文档数量、路径、索引编号或当前进度时，AI 必须读取本地文件核实。

## 二、两个入口

- Claude Desktop 入口：`Claude_Projects指令V3.6.md`（内容版本 V3.7）。
- Codex 入口：`AGENTS.md`。

两个入口可以规定各自产品的操作方式，但共同的角色边界、事实源、任务流转和治理原则以本协议为准。发生冲突时，应先报告冲突，不得静默选择对自己方便的版本。

## 三、角色分工

### 松阳：最终裁决者

- 确定重大观点、价值判断和发布方向。
- 授权提交、推送、发布、删除和跨仓库写入。
- 裁决 Claude 与 Codex 无法自行解决的分歧。

### Claude：管理者与内容验收者

- 讨论洞察、战略、立场和写作方向。
- 读取本地知识库，做理论映射、查重和证据审查。
- 确定任务目标、内容裁决、边界和验收标准。
- 创建与管理协作任务。
- 阅读实际修改文件并完成内容验收。

Claude 默认不写入正式知识正文、索引、代码和治理母版。用户明确要求时可以例外，但必须留下变更记录并建议 Codex 验证。

### Codex：默认写入者与执行验证者

- 读取本地最新文件和任务单。
- 修改正文、索引、代码、脚本和治理文件。
- 处理必要的关联更新。
- 运行验证并如实记录结果。
- 把完成结果提交给 Claude 或松阳验收。

## 四、单写入者原则

同一时间，同一正式文件只能由一个主体负责写入。默认写入者是 Codex。

Claude 可以写入以下管理文件：

- `协作任务/` 中的任务单和验收意见。
- `项目状态.md` 中的管理状态。

Claude 与 Codex 不得同时编辑同一任务的正式目标文件。发现对方已有未完成修改时，应先停下并说明。

## 五、任务生命周期

```text
讨论与裁决
    ↓
待执行
    ↓
执行中
    ↓
待验收
    ├─ 通过 → 已完成
    ├─ 小修 → 待执行
    └─ 需裁决 → 松阳决定
```

任务目录：

- `协作任务/待执行/`
- `协作任务/执行中/`
- `协作任务/待验收/`
- `协作任务/已完成/`

任务单是跨工具交接的上下文载体。任务必须做到：即使没有原聊天记录，另一方也能理解目标、边界和验收标准。

## 六、任务单最低要求

每个任务至少包含：

1. 唯一任务 ID 和标题。
2. 任务目标。
3. 背景与已经确认的裁决。
4. 目标文件或目标范围。
5. 必须修改。
6. 不得修改。
7. 验收标准。
8. 权限边界。
9. 不确定项与停止条件。
10. Codex 执行报告。
11. Claude/松阳验收记录。

## 七、事实与内容质量

- 不虚构研究、数据、人物、对话、引语、案例或项目文件。
- 公开案例和时效性事实必须可核查。
- 虚构内容必须明确标注“示意”。
- 项目观点、外部事实、AI 推断和行动建议必须能够区分。
- 真实与准确优先于标题冲击力、金句和反常识效果。
- 新洞察先查重、再判断是补充、冲突、空白还是已有理论的重述。

## 八、知识与双库理论治理

跨文档操作前读取 `_cross_refs.yaml`，遵守 `双库治理协议.md`；涉及 `D:\SK\theory` 时再读取其 `_README-本目录状态.md`：

- Master 有且只有一个。
- Reference 不得变成隐性第二母版。
- Mirror 是过渡态，不得长期增加。
- SK-native 逐篇审查，不整体迁移。

SK 仅 `theory/` 纳管；`core/`、`cases/` 和其他项目不纳管。Master 身份与理论成熟度是两条独立轴，不得互相推断。

CEET 是外部研究参考，不属于当前治理或同步范围。SK/theory 冻结新增正式编号与完整理论正文；新洞察统一进入 `D:\SK\ops\理论候选.md`，正式立卷发生在 sanxiangwendao。协议正文只在 sanxiangwendao 保留一份，SK 只保留指针。

新文档启动前判断根基、读者和成立判据。边界不一致时，拆分理论母版与应用版本，用指针关联。

## 九、Git 与高影响动作

本地编辑和可逆验证属于正常执行。以下动作必须得到松阳明确授权：

- Git commit 和 push。
- 发布到公开渠道。
- 删除正式文件或历史资料。
- 跨仓库写入和同步。
- 覆盖存在冲突的用户修改。

## 十、状态真实性

必须准确区分：

- 已讨论。
- 已裁决。
- 已创建任务。
- 已修改文件。
- 已通过验证。
- 已通过内容验收。
- 已更新 RAG。
- 已提交或推送。

任何阶段都不得越级声称完成。

## 十一、协议维护

本协议只放跨工具稳定规则。Claude 和 Codex 的产品专属操作方式分别留在各自入口文件中。

修改本协议相当于修改协作宪法，应说明原因、影响和版本变化。单次任务偏好不写入本协议。
